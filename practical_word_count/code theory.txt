# Complete Deep Explanation of Hadoop WordCount Mapper Code

You are learning one of the most famous programs in Hadoop:

# WordCount Mapper

This is the foundation of:

* Hadoop
* MapReduce
* Distributed processing
* Big Data programming

The mapper code is also similar to the one present in your uploaded PDF assignment. 

Now we will understand:

* every word
* every symbol
* every class
* every concept
* internal working
* memory behavior
* Hadoop execution flow
* how mapper runs internally
* why each thing is used

VERY deeply.

---

# COMPLETE CODE

```java
import java.io.IOException;
import org.apache.hadoop.io.*;
import org.apache.hadoop.mapred.*;

public class WCMapper extends MapReduceBase
implements Mapper<LongWritable, Text, Text, IntWritable> {

    public void map(LongWritable key, Text value,
                    OutputCollector<Text, IntWritable> output,
                    Reporter rep) throws IOException {

        String line = value.toString();

        for(String word : line.split(" ")) {
            if(word.length() > 0) {
                output.collect(new Text(word),
                               new IntWritable(1));
            }
        }
    }
}
```

---

# STEP 1 — What is This Program?

This program is a:

# Hadoop Mapper Program

Purpose:

* Read input text
* Split into words
* Generate key-value pairs

Example:

Input:

```text id="mq4tsw"
cat dog cat
```

Output:

```text id="dcv1mq"
(cat,1)
(dog,1)
(cat,1)
```

This output is called:

# Intermediate Output

Reducer later counts frequencies.

---

# STEP 2 — Understanding the Big Picture

MapReduce has phases:

```text id="kw7u5n"
Input File
   ↓
Mapper
   ↓
Shuffle + Sort
   ↓
Reducer
   ↓
Final Output
```

This code belongs ONLY to:

# Mapper Phase

---

# STEP 3 — First Import

```java
import java.io.IOException;
```

---

# What is import?

`import` means:

> “Bring another class/package into this program.”

Java stores classes in packages.

Without import:

* Java cannot recognize external classes.

---

# What is java.io?

`io` means:

# Input / Output

Used for:

* reading files
* writing files
* streams
* exceptions

---

# What is IOException?

# Exception related to input-output operations

Example problems:

* file not found
* disk error
* network interruption

---

# Why Hadoop Needs IOException?

Hadoop works with:

* distributed files
* HDFS
* network communication
* disk reading

Many failures possible.

So mapper declares:

```java
throws IOException
```

meaning:

> “This method may produce IO-related errors.”

---

# STEP 4 — Second Import

```java
import org.apache.hadoop.io.*;
```

---

# What is org.apache.hadoop.io?

Hadoop package containing:

* special Hadoop data types

---

# Why Hadoop Uses Its Own Data Types?

Java primitive types:

```java
int
String
long
```

cannot efficiently work in:

* distributed systems
* serialization
* network transfer

Hadoop uses optimized writable objects.

---

# Serialization Concept

VERY IMPORTANT.

---

# What is Serialization?

Converting object into:

* byte stream

so it can:

* travel over network
* store in disk
* send between nodes

---

# Example

Object:

```java
IntWritable(5)
```

Serialized:

```text id="vuj9n6"
00000101
```

---

# Hadoop Writable Classes

| Hadoop Type     | Java Equivalent |
| --------------- | --------------- |
| IntWritable     | int             |
| LongWritable    | long            |
| Text            | String          |
| BooleanWritable | boolean         |

---

# Why Writable Important?

Because Hadoop:

* transfers data across nodes
* stores intermediate output
* shuffles network data

Efficient serialization is critical.

---

# STEP 5 — Third Import

```java
import org.apache.hadoop.mapred.*;
```

---

# What is mapred?

Contains:

* MapReduce framework classes

Like:

* Mapper
* Reducer
* JobConf
* OutputCollector

---

# Why * ?

```java
*
```

means:

# import everything

from package.

---

# STEP 6 — Class Declaration

```java
public class WCMapper
```

---

# public

Accessible everywhere.

---

# class

Blueprint/template for object creation.

---

# WCMapper

Class name.

Meaning:

```text id="qq5p1h"
Word Count Mapper
```

---

# STEP 7 — extends

```java
extends MapReduceBase
```

---

# What is inheritance?

One class acquiring properties of another class.

---

# MapReduceBase

Provided by Hadoop.

Contains:

* default implementations
* helper methods

---

# Why extend MapReduceBase?

To avoid manually implementing unnecessary methods.

It simplifies coding.

---

# STEP 8 — implements

```java
implements Mapper
```

---

# Difference Between extends and implements

| extends        | implements           |
| -------------- | -------------------- |
| Inherits class | Implements interface |

---

# What is Interface?

A contract.

It says:

> “If you use me, you MUST define these methods.”

---

# Mapper Interface

Requires:

```java
map()
```

method.

---

# STEP 9 — Generic Types

```java
Mapper<LongWritable, Text, Text, IntWritable>
```

VERY IMPORTANT.

---

# Structure

```java
Mapper<InputKey, InputValue, OutputKey, OutputValue>
```

---

# Actual Types

```java
Mapper<
LongWritable,
Text,
Text,
IntWritable
>
```

---

# Meaning

| Type         | Meaning      |
| ------------ | ------------ |
| LongWritable | Input key    |
| Text         | Input value  |
| Text         | Output key   |
| IntWritable  | Output value |

---

# Input Key

```java
LongWritable
```

Represents:

# byte offset of line

---

# Example

Suppose file:

```text id="9pq5oo"
Hello World
Hadoop Rocks
```

Internally:

| Line         | Offset |
| ------------ | ------ |
| Hello World  | 0      |
| Hadoop Rocks | 12     |

---

# Input Value

```java
Text
```

Actual line content.

Example:

```text id="nl35lm"
Hello World
```

---

# Output Key

```java
Text
```

Word itself.

Example:

```text id="4gggv0"
cat
```

---

# Output Value

```java
IntWritable
```

Count:

```text id="4m6d1o"
1
```

---

# STEP 10 — Opening Curly Brace

```java
{
```

Starts class body.

---

# STEP 11 — map Method

```java
public void map(
```

---

# Why public?

Hadoop framework must access it.

---

# Why void?

Returns nothing directly.

Output goes through:

```java
output.collect()
```

---

# map()

Main mapper function.

Hadoop automatically calls it.

---

# VERY IMPORTANT

You NEVER manually call:

```java
map()
```

Hadoop framework calls it internally.

---

# STEP 12 — Parameters

---

# Parameter 1

```java
LongWritable key
```

---

# Meaning

Byte position of line in file.

---

# Example

```text id="6zmtqt"
0 → first line
20 → second line
50 → third line
```

---

# Why Needed?

Allows Hadoop to:

* track positions
* split files
* parallelize processing

---

# Parameter 2

```java
Text value
```

Actual line content.

Example:

```text id="x43cmg"
Hello Hadoop
```

---

# Parameter 3

```java
OutputCollector<Text, IntWritable> output
```

VERY IMPORTANT.

---

# What is OutputCollector?

Used to emit:

# intermediate key-value pairs

---

# Example

```java
output.collect(
   new Text("cat"),
   new IntWritable(1)
);
```

Produces:

```text id="zqjz7v"
(cat,1)
```

---

# Parameter 4

```java
Reporter rep
```

Used for:

* reporting progress
* debugging
* counters
* status updates

---

# Why Reporter Needed?

In huge clusters:

* tasks may take hours.

Reporter tells Hadoop:

```text id="9w3uxl"
"I'm still alive"
```

Otherwise Hadoop may think:

* task failed.

---

# STEP 13 — throws IOException

```java
throws IOException
```

Means:

* map method may produce IO errors.

Hadoop handles exception.

---

# STEP 14 — Convert Text to String

```java
String line = value.toString();
```

---

# value

Currently:

```java
Text
```

Hadoop object.

---

# Why toString()?

Need normal Java String operations.

Converts:

```java
Text
```

into:

```java
String
```

---

# Example

Before:

```java
Text("Hello")
```

After:

```java
"Hello"
```

---

# STEP 15 — for Loop

```java
for(String word : line.split(" "))
```

VERY IMPORTANT.

---

# Enhanced For Loop

Also called:

# for-each loop

---

# line.split(" ")

Splits sentence using spaces.

---

# Example

```java
"cat dog elephant"
```

becomes:

```text id="h4qjfd"
["cat","dog","elephant"]
```

---

# Internal Working of split()

Uses:

# Regular Expressions

Separator:

```java
" "
```

means:
space character.

---

# STEP 16 — Loop Iteration

Each iteration processes one word.

---

# Example

Iteration 1:

```text id="tv9l5v"
word = cat
```

Iteration 2:

```text id="2e8z3z"
word = dog
```

---

# STEP 17 — if Condition

```java
if(word.length() > 0)
```

---

# Why Needed?

Avoid empty words.

---

# Example Problem

Sentence:

```text id="b87o7h"
cat    dog
```

Multiple spaces create:

```text id="qnhyde"
""
```

empty strings.

This condition ignores them.

---

# word.length()

Returns number of characters.

---

# STEP 18 — output.collect()

```java
output.collect(
    new Text(word),
    new IntWritable(1)
);
```

MOST IMPORTANT LINE.

---

# What Happens Here?

Mapper emits:

# intermediate key-value pair

---

# Example

If:

```java
word = "cat"
```

Output:

```text id="x7xg4y"
(cat,1)
```

---

# new Text(word)

Creates Hadoop Text object.

---

# new IntWritable(1)

Creates Hadoop integer object.

---

# Why Always 1?

Because:

* each occurrence counted once.

Reducer later sums them.

---

# Internal Working

Hadoop stores mapper output into:

* memory buffer

When buffer full:

* spills to disk.

Then:

* shuffle phase begins.

---

# STEP 19 — Closing Braces

---

## First }

Ends:

```java
if
```

---

## Second }

Ends:

```java
for loop
```

---

## Third }

Ends:

```java
map method
```

---

## Fourth }

Ends:

```java
class
```

---

# COMPLETE INTERNAL EXECUTION FLOW

Now understand EXACTLY what happens internally.

---

# INPUT FILE

```text id="09f3r7"
cat dog cat
```

---

# STEP 1 — HDFS Split

File split into blocks.

---

# STEP 2 — Mapper Assigned

One mapper processes block.

---

# STEP 3 — Hadoop Calls map()

Internally:

```java
map(0,"cat dog cat")
```

---

# STEP 4 — toString()

```java
line = "cat dog cat"
```

---

# STEP 5 — split()

Creates array:

```text id="4f4h4o"
["cat","dog","cat"]
```

---

# STEP 6 — Loop Runs

---

## Iteration 1

```java
word = cat
```

Output:

```text id="teek2q"
(cat,1)
```

---

## Iteration 2

```java
word = dog
```

Output:

```text id="1ryhsn"
(dog,1)
```

---

## Iteration 3

```java
word = cat
```

Output:

```text id="7ktx5k"
(cat,1)
```

---

# Final Mapper Output

```text id="wjlwmc"
(cat,1)
(dog,1)
(cat,1)
```

---

# What Happens Next?

# Shuffle + Sort Phase

Hadoop groups:

```text id="z0a8ud"
(cat,[1,1])
(dog,[1])
```

Reducer later sums values.

---

# VERY IMPORTANT HADOOP CONCEPTS USED

| Concept                | Used Where        |
| ---------------------- | ----------------- |
| Serialization          | Writable classes  |
| Distributed Processing | Multiple mappers  |
| Parallelism            | Block processing  |
| Fault Tolerance        | Hadoop framework  |
| Key-Value Model        | collect()         |
| Tokenization           | split()           |
| Intermediate Data      | Mapper output     |
| Data Locality          | Mapper near block |

---

# Why Mapper is Powerful

Mapper:

* transforms raw data
* prepares distributed computation
* enables parallel processing

Without mapper:

* Hadoop cannot scale.

---

# MEMORY BEHAVIOR INSIDE MAPPER

Internally:

```text id="0kqodf"
Input Split
   ↓
RecordReader
   ↓
key,value
   ↓
map()
   ↓
Memory Buffer
   ↓
Spill to Disk
   ↓
Shuffle
```

---

# RecordReader

Converts raw file into:

```java
(key,value)
```

pairs.

---

# Example

```text id="4wt6ur"
0 → "Hello World"
12 → "Hadoop Rocks"
```

---

# FINAL UNDERSTANDING

This small mapper code demonstrates:

* distributed computing
* serialization
* tokenization
* parallel processing
* key-value architecture
* Hadoop execution model
* data transformation
* fault-tolerant processing

It is one of the foundational programs of Big Data systems.
-------------------------------------------------------------------------------------------
# Complete Deep Explanation of Hadoop Reducer Code

Now you are learning the:

# Reducer Phase of Hadoop MapReduce

This is the second major phase after:

# Mapper

Your reducer code is also based on the same Hadoop WordCount assignment present in your uploaded PDF. 

This reducer performs:

# aggregation

It combines all values belonging to the same key.

We will now deeply understand:

* every word
* every line
* every symbol
* every class
* internal Hadoop behavior
* memory working
* shuffle relation
* iterator concept
* reducer lifecycle
* execution flow
* network transfer
* serialization
* grouping mechanism
* complete reducer architecture

from absolute beginning to advanced level.

---

# COMPLETE CODE

```java id="d6w36v"
import java.io.IOException;
import java.util.Iterator;
import org.apache.hadoop.io.*;
import org.apache.hadoop.mapred.*;

public class WCReducer extends MapReduceBase
implements Reducer<Text, IntWritable, Text, IntWritable> {

    public void reduce(Text key, Iterator<IntWritable> values,
                       OutputCollector<Text, IntWritable> output,
                       Reporter rep) throws IOException {

        int count = 0;

        while(values.hasNext()) {
            count += values.next().get();
        }

        output.collect(key, new IntWritable(count));
    }
}
```

---

# STEP 1 — What is Reducer?

Reducer is the:

# aggregation phase

It receives:

```text id="6cfjqh"
(key, list_of_values)
```

and produces:

```text id="9hz26h"
(final_key, final_value)
```

---

# Example

Mapper output:

```text id="1b5r7x"
(cat,1)
(cat,1)
(cat,1)
(dog,1)
```

After shuffle/sort:

```text id="1zl0oc"
(cat,[1,1,1])
(dog,[1])
```

Reducer output:

```text id="nh0x4m"
(cat,3)
(dog,1)
```

---

# STEP 2 — First Import

```java id="0yd7az"
import java.io.IOException;
```

---

# Why Used?

Reducer may:

* read/write network data
* access HDFS
* transfer intermediate output

These operations may fail.

So:

```java id="j8iz3k"
IOException
```

handles input-output errors.

---

# STEP 3 — Second Import

```java id="0jagjl"
import java.util.Iterator;
```

VERY IMPORTANT.

---

# What is Iterator?

Iterator is a Java object used to:

# traverse elements one-by-one

---

# Example

Suppose:

```text id="t3gtrt"
[1,1,1,1]
```

Iterator allows accessing:

```text id="ykkvcw"
first element
second element
third element
...
```

one at a time.

---

# Why Hadoop Uses Iterator?

Because:

* data may be HUGE
* millions of values possible

Hadoop cannot load everything at once into memory.

Iterator enables:

# streaming access

---

# Streaming Concept

Instead of:

```text id="h7kj0q"
load all values in RAM
```

Hadoop processes:

```text id="7d4n44"
one value at a time
```

This saves memory.

---

# STEP 4 — Hadoop IO Import

```java id="0o9y9h"
import org.apache.hadoop.io.*;
```

Imports Hadoop writable data types.

---

# Writable Types

| Hadoop Type  | Java Equivalent |
| ------------ | --------------- |
| Text         | String          |
| IntWritable  | int             |
| LongWritable | long            |

---

# Why Hadoop Uses Writable?

Because Hadoop needs:

* serialization
* network transfer
* distributed processing

Writable classes are optimized for:

* speed
* compact storage
* distributed systems

---

# STEP 5 — MapReduce Import

```java id="wvz70k"
import org.apache.hadoop.mapred.*;
```

Imports:

* Reducer
* OutputCollector
* Reporter
* MapReduceBase

---

# STEP 6 — Class Declaration

```java id="6ow7dr"
public class WCReducer
```

---

# public

Accessible everywhere.

---

# class

Blueprint/template.

---

# WCReducer

Meaning:

```text id="yjk5d9"
Word Count Reducer
```

---

# STEP 7 — extends MapReduceBase

```java id="wyld0q"
extends MapReduceBase
```

---

# Why extend?

Provides default functionality.

Avoids writing unnecessary methods.

---

# STEP 8 — implements Reducer

```java id="s1l64u"
implements Reducer
```

---

# What is Reducer Interface?

A contract.

Requires:

```java id="0v3n1v"
reduce()
```

method implementation.

---

# STEP 9 — Generic Types

```java id="4mwyi6"
Reducer<Text, IntWritable, Text, IntWritable>
```

VERY IMPORTANT.

---

# Structure

```java id="zhynv4"
Reducer<InputKey,
        InputValue,
        OutputKey,
        OutputValue>
```

---

# Actual Meaning

| Type        | Meaning      |
| ----------- | ------------ |
| Text        | Input key    |
| IntWritable | Input values |
| Text        | Output key   |
| IntWritable | Output value |

---

# Reducer Input

Reducer receives:

```text id="jlwm8t"
(word, [1,1,1,1])
```

---

# Example

```text id="g2m9j1"
(cat,[1,1,1])
```

---

# Reducer Output

```text id="96a6tb"
(cat,3)
```

---

# STEP 10 — reduce Method

```java id="5lc2i2"
public void reduce(
```

---

# Hadoop Automatically Calls reduce()

You NEVER manually call:

```java id="s5pbz8"
reduce()
```

Hadoop framework does it internally.

---

# STEP 11 — Parameter 1

```java id="cfy69v"
Text key
```

Represents:

# unique word

Example:

```text id="e4jlwm"
cat
```

---

# IMPORTANT

Reducer processes:

# ONE KEY AT A TIME

---

# Example Flow

Hadoop calls:

```java id="glp1l8"
reduce("cat",[1,1,1])
```

Then:

```java id="abthry"
reduce("dog",[1,1])
```

---

# STEP 12 — Parameter 2

```java id="f4vb0p"
Iterator<IntWritable> values
```

VERY VERY IMPORTANT.

---

# What is values?

All counts belonging to same key.

---

# Example

For:

```text id="6m3g2c"
(cat,1)
(cat,1)
(cat,1)
```

Reducer receives:

```text id="n72w3t"
(cat,[1,1,1])
```

---

# Why Iterator?

Suppose:

* billions of occurrences

Cannot store everything in RAM.

Iterator streams values efficiently.

---

# Internal Hadoop Behavior

During shuffle:

* grouped values stored
* reducer accesses sequentially

using iterator.

---

# STEP 13 — OutputCollector

```java id="r7i1z7"
OutputCollector<Text, IntWritable> output
```

Used to emit:

# final output

---

# Example

```java id="ig4vmg"
output.collect(
   new Text("cat"),
   new IntWritable(3)
);
```

Produces:

```text id="8ay3pq"
(cat,3)
```

---

# STEP 14 — Reporter

```java id="f2frjs"
Reporter rep
```

Used for:

* progress updates
* debugging
* counters
* monitoring

---

# Why Needed?

Reducers may run for hours.

Reporter tells Hadoop:

```text id="wskmha"
"I'm still working"
```

Otherwise Hadoop may think reducer crashed.

---

# STEP 15 — throws IOException

```java id="x3l41r"
throws IOException
```

Reducer may:

* write output
* access network
* communicate with HDFS

Errors possible.

---

# STEP 16 — Initialize Counter

```java id="1mq4si"
int count = 0;
```

---

# Purpose

Stores total frequency.

---

# Example

For:

```text id="tbvsyo"
(cat,[1,1,1])
```

count eventually becomes:

```text id="vtj8qs"
3
```

---

# STEP 17 — while Loop

```java id="9nqbx4"
while(values.hasNext())
```

VERY IMPORTANT.

---

# hasNext()

Checks:

```text id="s9edjg"
"Is another value available?"
```

Returns:

* true
* false

---

# Example

Iterator:

```text id="k1hn0n"
[1,1,1]
```

---

## Iteration 1

Available?

```text id="9a8pd7"
true
```

---

## Iteration 2

Available?

```text id="m50we4"
true
```

---

## Iteration 3

Available?

```text id="c7o9zv"
true
```

---

## End

Available?

```text id="gukv6f"
false
```

Loop stops.

---

# STEP 18 — values.next()

```java id="hm2l1x"
values.next()
```

Retrieves next value.

---

# Example

Iterator:

```text id="1zl1kt"
[1,1,1]
```

Calls:

```java id="h0x8xg"
next()
```

returns:

```text id="hdq5qt"
1
```

---

# STEP 19 — .get()

```java id="eqw2hs"
.get()
```

VERY IMPORTANT.

---

# Why Needed?

`values.next()` returns:

```java id="uw1evw"
IntWritable
```

NOT normal int.

Need:

```java id="r81qeq"
.get()
```

to extract primitive integer.

---

# Example

Before:

```java id="d8q5aj"
IntWritable(1)
```

After:

```java id="gwk6s2"
1
```

---

# STEP 20 — count +=

```java id="7m4hvs"
count += values.next().get();
```

Equivalent to:

```java id="pdsl07"
count = count + value;
```

---

# Example Execution

Initial:

```text id="vm3u1l"
count = 0
```

---

## First Iteration

Value:

```text id="n9p0cv"
1
```

count:

```text id="ibg0du"
1
```

---

## Second Iteration

count:

```text id="hyh32g"
2
```

---

## Third Iteration

count:

```text id="c48a0e"
3
```

---

# STEP 21 — output.collect()

```java id="5o56x4"
output.collect(key, new IntWritable(count));
```

MOST IMPORTANT REDUCER LINE.

---

# Purpose

Emit:

# final word count

---

# Example

If:

```text id="67suxj"
key = cat
count = 3
```

Output:

```text id="o2ocbp"
(cat,3)
```

---

# new IntWritable(count)

Converts normal int into Hadoop writable object.

---

# Why Writable Again?

Because reducer output may:

* move over network
* store in HDFS
* serialize to disk

---

# COMPLETE INTERNAL FLOW

Now understand EXACTLY what Hadoop does internally.

---

# INPUT FILE

```text id="lf1jl7"
cat dog cat
```

---

# STEP 1 — Mapper Output

```text id="2k9m3m"
(cat,1)
(dog,1)
(cat,1)
```

---

# STEP 2 — Shuffle Phase

Hadoop groups same keys.

```text id="bqmkbx"
(cat,[1,1])
(dog,[1])
```

VERY IMPORTANT:

> Shuffle is automatic.

You do NOT write shuffle code.

---

# STEP 3 — Sort Phase

Keys sorted alphabetically.

```text id="1dr2xn"
cat
dog
```

---

# STEP 4 — Hadoop Calls Reducer

Internally:

```java id="0lm9hl"
reduce("cat",[1,1])
```

---

# STEP 5 — count Initialization

```java id="5jmgm7"
count = 0
```

---

# STEP 6 — Loop Execution

---

## Iteration 1

```java id="c2pk29"
count = 0 + 1
```

count:

```text id="6x7f95"
1
```

---

## Iteration 2

```java id="x2kic8"
count = 1 + 1
```

count:

```text id="53t6gh"
2
```

---

# STEP 7 — Emit Output

```text id="n5c7oe"
(cat,2)
```

---

# STEP 8 — Next Key

Hadoop calls:

```java id="upzb7y"
reduce("dog",[1])
```

---

# FINAL OUTPUT

```text id="pvh5iw"
(cat,2)
(dog,1)
```

---

# HOW SHUFFLE CONNECTS TO REDUCER

VERY IMPORTANT CONCEPT.

---

# Mapper Output

Distributed across nodes.

---

# Shuffle Phase Does

## 1. Transfers Data

Across network.

---

## 2. Groups Same Keys

```text id="xzn73n"
(cat,[1,1,1])
```

---

## 3. Sorts Keys

Alphabetically.

---

## 4. Sends to Reducer

Reducer receives grouped data.

---

# INTERNAL MEMORY BEHAVIOR

Reducer internally:

```text id="qvgxvk"
Shuffle Buffer
      ↓
Merge
      ↓
Sort
      ↓
Iterator Stream
      ↓
reduce()
      ↓
Output File
```

---

# WHY ITERATOR IS CRITICAL

Suppose word:

```text id="msy9bm"
the
```

appears:

```text id="db32rr"
500 million times
```

Loading all values into RAM impossible.

Iterator enables:

```text id="bn94c9"
stream processing
```

one-by-one.

---

# WHY REDUCER IS POWERFUL

Reducer performs:

* aggregation
* summarization
* analytics
* final computation

Without reducer:

* no meaningful result generation.

---

# CONCEPTS USED IN THIS CODE

| Concept               | Used Where        |
| --------------------- | ----------------- |
| Aggregation           | count variable    |
| Iteration             | Iterator          |
| Serialization         | Writable classes  |
| Distributed Computing | Hadoop framework  |
| Parallelism           | Multiple reducers |
| Streaming             | Iterator access   |
| Key-Value Processing  | collect()         |
| Fault Tolerance       | Hadoop runtime    |
| Shuffle & Sort        | Pre-reducer phase |

---

# REAL REDUCER EXECUTION MODEL

```text id="m1uxr4"
Mapper Output
     ↓
Partitioner
     ↓
Shuffle
     ↓
Sort
     ↓
Grouping
     ↓
Reducer
     ↓
HDFS Output
```

---

# FINAL UNDERSTANDING

This reducer code demonstrates:

* distributed aggregation
* iterator-based streaming
* Hadoop serialization
* network-based data grouping
* parallel reduction
* memory-efficient processing
* key-value architecture
* scalable computation

This is one of the core foundations of:

* Big Data
* Hadoop
* Distributed Systems
* Large-scale analytics
* Parallel computing
-----------------------------------------------------------------------------------------------
# Complete Deep Explanation of Hadoop Driver Code

Now you are learning the:

# Driver Program of Hadoop MapReduce

This is the MOST IMPORTANT coordinating file.

Without Driver:

* Mapper will not run
* Reducer will not run
* Hadoop job cannot start
* Configuration cannot happen

The driver acts like:

# the brain/controller of the MapReduce job

The structure is similar to the Hadoop WordCount driver shown in your uploaded assignment PDF. 

Now we will deeply understand:

* every line
* every word
* every class
* job lifecycle
* configuration
* Hadoop internals
* YARN interaction
* cluster execution
* task scheduling
* HDFS communication
* how mapper/reducer connect
* how job submission works
* complete execution architecture

from absolute beginner to advanced internal level.

---

# COMPLETE CODE

```java id="8up85z"
import java.io.IOException;
import org.apache.hadoop.conf.Configured;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.*;
import org.apache.hadoop.mapred.*;
import org.apache.hadoop.util.*;

public class WCDriver extends Configured implements Tool {

    public int run(String args[]) throws IOException {

        JobConf conf = new JobConf(WCDriver.class);

        FileInputFormat.setInputPaths(conf, new Path(args[0]));
        FileOutputFormat.setOutputPath(conf, new Path(args[1]));

        conf.setMapperClass(WCMapper.class);
        conf.setReducerClass(WCReducer.class);

        conf.setMapOutputKeyClass(Text.class);
        conf.setMapOutputValueClass(IntWritable.class);

        conf.setOutputKeyClass(Text.class);
        conf.setOutputValueClass(IntWritable.class);

        JobClient.runJob(conf);

        return 0;
    }

    public static void main(String args[]) throws Exception {
        ToolRunner.run(new WCDriver(), args);
    }
}
```

---

# STEP 1 — What is Driver Program?

The Driver Program is responsible for:

| Responsibility    | Meaning                        |
| ----------------- | ------------------------------ |
| Job Configuration | Set mapper/reducer             |
| Input Path        | Tell Hadoop where input exists |
| Output Path       | Tell Hadoop where output goes  |
| Job Submission    | Send job to cluster            |
| Resource Setup    | Configure execution            |
| Execution Control | Start MapReduce pipeline       |

---

# Real-Life Analogy

Suppose:

* Mapper = workers
* Reducer = accountants

Then:

# Driver = manager

The manager:

* assigns work
* gives addresses
* starts process
* coordinates execution

---

# STEP 2 — First Import

```java id="h3d0hl"
import java.io.IOException;
```

---

# Why Needed?

Hadoop operations may fail because of:

* HDFS issues
* network problems
* cluster failures
* file access errors

So driver handles:

```java id="jlwmt4"
IOException
```

---

# STEP 3 — Configured Import

```java id="6wgbka"
import org.apache.hadoop.conf.Configured;
```

VERY IMPORTANT.

---

# What is Configuration?

Configuration means:

# settings for Hadoop job

Examples:

* mapper class
* reducer class
* memory allocation
* input path
* output path

---

# What is Configured Class?

Provides:

```text id="2it6g9"
configuration management
```

for Hadoop applications.

---

# Why Extend Configured?

Allows easy access to:

* Hadoop configuration
* cluster settings
* runtime properties

---

# STEP 4 — Path Import

```java id="h8u1hx"
import org.apache.hadoop.fs.Path;
```

---

# What is Path?

Represents:

# file path in Hadoop

---

# Example

```java id="h2gv7x"
new Path("/input/data.txt")
```

---

# Why Hadoop Uses Path Instead of String?

Because Hadoop supports:

* HDFS
* local FS
* cloud storage
* distributed file systems

Path abstracts filesystem handling.

---

# STEP 5 — Hadoop IO Import

```java id="9jv3wt"
import org.apache.hadoop.io.*;
```

Imports Hadoop writable types:

* Text
* IntWritable
* LongWritable

---

# STEP 6 — mapred Import

```java id="2pnt0i"
import org.apache.hadoop.mapred.*;
```

Imports:

* JobConf
* JobClient
* FileInputFormat
* FileOutputFormat

---

# STEP 7 — util Import

```java id="w0jv7q"
import org.apache.hadoop.util.*;
```

Imports:

* Tool
* ToolRunner

---

# STEP 8 — Class Declaration

```java id="0zv9ku"
public class WCDriver
```

---

# Meaning

```text id="5mtdgr"
Word Count Driver
```

---

# STEP 9 — extends Configured

```java id="x7dn0m"
extends Configured
```

Allows:

* Hadoop configuration support

---

# STEP 10 — implements Tool

```java id="hdb4iq"
implements Tool
```

VERY IMPORTANT.

---

# What is Tool Interface?

Standard Hadoop interface for:

* running jobs
* handling configurations

---

# Tool Interface Requires

```java id="n8f3qa"
run()
```

method implementation.

---

# Why Tool Important?

Provides:

* cleaner Hadoop job execution
* generic configuration support
* command-line parsing

---

# STEP 11 — run Method

```java id="6j8ixt"
public int run(String args[])
```

VERY IMPORTANT.

---

# args[]

Stores command-line arguments.

---

# Example

When running:

```bash id="x5v52e"
hadoop jar wc.jar input output
```

---

# args Values

| Index   | Value  |
| ------- | ------ |
| args[0] | input  |
| args[1] | output |

---

# Why int Return Type?

Indicates:

* success
* failure

Usually:

```text id="hhqvlr"
0 = success
-1 = failure
```

---

# STEP 12 — Create JobConf

```java id="6c8mhz"
JobConf conf = new JobConf(WCDriver.class);
```

MOST IMPORTANT LINE.

---

# What is JobConf?

# Job Configuration Object

Stores ALL job settings.

---

# Internal Role of JobConf

Contains:

* mapper info
* reducer info
* input/output paths
* memory settings
* task settings
* cluster settings

---

# Why WCDriver.class?

Tells Hadoop:

```text id="3o8d8l"
which class contains job configuration
```

---

# Internally

Hadoop creates:

# complete execution plan

from this object.

---

# STEP 13 — Set Input Path

```java id="yq1bmb"
FileInputFormat.setInputPaths(conf,
new Path(args[0]));
```

VERY IMPORTANT.

---

# Purpose

Tells Hadoop:

```text id="u95g5h"
where input file exists
```

---

# Example

```bash id="93fsvj"
input/
```

---

# What Happens Internally?

Hadoop:

1. Reads input location
2. Accesses HDFS
3. Splits file into blocks
4. Creates input splits

---

# Input Splitting

Suppose:

```text id="uwvf6m"
1 GB file
```

Block size:

```text id="m2shxp"
128 MB
```

Creates:

```text id="ln2pwo"
8 splits
```

Usually:

```text id="jlwmr7"
8 mappers
```

---

# STEP 14 — Set Output Path

```java id="4vjlwm"
FileOutputFormat.setOutputPath(
conf,
new Path(args[1])
);
```

---

# Purpose

Tells Hadoop:

```text id="x2vymx"
where final reducer output should go
```

---

# IMPORTANT RULE

Output directory:

# must NOT already exist

Otherwise Hadoop throws error.

---

# Why?

To prevent accidental overwrite.

---

# STEP 15 — Set Mapper Class

```java id="yfqjz5"
conf.setMapperClass(WCMapper.class);
```

VERY IMPORTANT.

---

# Purpose

Registers:

# mapper implementation

---

# Internally

Hadoop now knows:

```text id="y07y6f"
which mapper to execute
```

---

# During Execution

Hadoop creates mapper objects:

```java id="7fbc6r"
new WCMapper()
```

automatically.

---

# STEP 16 — Set Reducer Class

```java id="jlwm2k"
conf.setReducerClass(WCReducer.class);
```

Registers reducer implementation.

---

# Internally

Hadoop later creates:

```java id="9r3om4"
new WCReducer()
```

objects.

---

# STEP 17 — Map Output Key Type

```java id="wjlwml"
conf.setMapOutputKeyClass(Text.class);
```

VERY IMPORTANT.

---

# Mapper Output

```text id="fl3wcf"
(word,1)
```

Key:

```text id="t07o1r"
word
```

Type:

```java id="2v5ahh"
Text
```

---

# Why Hadoop Needs Type Info?

For:

* serialization
* shuffle
* sorting
* memory allocation
* network transfer

---

# STEP 18 — Map Output Value Type

```java id="9x7l2r"
conf.setMapOutputValueClass(
IntWritable.class
);
```

Value:

```text id="9k4zhs"
1
```

Type:

```java id="jlwm9q"
IntWritable
```

---

# STEP 19 — Final Output Key Type

```java id="jlwmhx"
conf.setOutputKeyClass(Text.class);
```

Reducer final output key type.

Example:

```text id="j2a5so"
cat
```

---

# STEP 20 — Final Output Value Type

```java id="jlwmvj"
conf.setOutputValueClass(
IntWritable.class
);
```

Reducer output value type.

Example:

```text id="3pwjlwm"
5
```

---

# STEP 21 — Run Job

```java id="7v8qto"
JobClient.runJob(conf);
```

MOST IMPORTANT EXECUTION LINE.

---

# What Happens Here?

This line:

# submits job to Hadoop cluster

---

# Internally HUGE THINGS HAPPEN

---

# INTERNAL EXECUTION FLOW

```text id="jlwmqt"
Driver
  ↓
ResourceManager
  ↓
Input Splits
  ↓
Mapper Tasks
  ↓
Shuffle & Sort
  ↓
Reducer Tasks
  ↓
Output to HDFS
```

---

# Detailed Internal Process

---

## STEP 1 — Job Submission

Driver sends:

```text id="jlwm5n"
JobConf
```

to Hadoop.

---

## STEP 2 — ResourceManager/YARN

Allocates:

* CPU
* memory
* containers

---

## STEP 3 — Input Splits Created

HDFS blocks analyzed.

---

## STEP 4 — Mapper Tasks Started

Each mapper processes split.

---

## STEP 5 — Mapper Output Generated

```text id="jlwmk7"
(word,1)
```

pairs.

---

## STEP 6 — Shuffle Phase

Groups identical keys.

---

## STEP 7 — Sort Phase

Sorts keys alphabetically.

---

## STEP 8 — Reducer Tasks Started

Reducers aggregate values.

---

## STEP 9 — Output Written

Stored into:

```text id="jlwm1r"
HDFS output directory
```

---

# STEP 22 — return 0

```java id="jlwmv0"
return 0;
```

Means:

# successful execution

---

# STEP 23 — main Method

```java id="f7jlwm"
public static void main(String args[])
```

Program entry point.

---

# static

Can run without object creation.

---

# main()

JVM starts execution here.

---

# STEP 24 — ToolRunner.run()

```java id="jlwm0v"
ToolRunner.run(new WCDriver(), args);
```

VERY IMPORTANT.

---

# What ToolRunner Does?

Handles:

* configuration setup
* argument parsing
* job launching

---

# Internal Flow

```text id="jlwmh8"
main()
   ↓
ToolRunner.run()
   ↓
run()
   ↓
Job Submission
```

---

# Why Use ToolRunner?

Simplifies:

* Hadoop execution
* configuration handling
* command-line processing

---

# COMPLETE EXECUTION FLOW

Now understand COMPLETE end-to-end flow.

---

# STEP 1 — User Runs Command

```bash id="8yjlwm"
hadoop jar wc.jar input output
```

---

# STEP 2 — JVM Starts

Calls:

```java id="jlwmun"
main()
```

---

# STEP 3 — ToolRunner Executes

Calls:

```java id="jlwmux"
run()
```

---

# STEP 4 — JobConf Created

Stores all configuration.

---

# STEP 5 — Input Path Registered

HDFS file located.

---

# STEP 6 — Output Path Registered

Output destination set.

---

# STEP 7 — Mapper/Reducer Registered

Framework knows execution classes.

---

# STEP 8 — Job Submission

Cluster execution begins.

---

# STEP 9 — HDFS Splitting

Input divided into blocks.

---

# STEP 10 — Mapper Execution

Parallel processing starts.

---

# STEP 11 — Shuffle & Sort

Grouping occurs.

---

# STEP 12 — Reducer Execution

Aggregation occurs.

---

# STEP 13 — Output Storage

Results written to HDFS.

---

# REAL INTERNAL ARCHITECTURE

```text id="jlwm40"
                DRIVER
                   |
             JobConf Object
                   |
          JobClient.runJob()
                   |
         -------------------
         |                 |
    ResourceManager    HDFS
         |
    Mapper Tasks
         |
   Shuffle & Sort
         |
    Reducer Tasks
         |
      Output File
```

---

# WHY DRIVER IS CRITICAL

Without Driver:

* Hadoop doesn't know mapper
* Hadoop doesn't know reducer
* no input path
* no output path
* no job submission

Driver coordinates EVERYTHING.

---

# IMPORTANT CONCEPTS USED

| Concept               | Used Where                  |
| --------------------- | --------------------------- |
| Configuration         | JobConf                     |
| Distributed Execution | runJob()                    |
| HDFS Path Management  | Path                        |
| Cluster Communication | JobClient                   |
| Serialization         | Writable classes            |
| Resource Management   | YARN                        |
| Task Scheduling       | Mapper/Reducer registration |
| Parallel Processing   | Input splits                |

---

# FINAL UNDERSTANDING

This driver code demonstrates:

* Hadoop job configuration
* cluster communication
* distributed execution management
* task scheduling
* HDFS interaction
* mapper/reducer orchestration
* job lifecycle management
* scalable distributed processing

It acts as the:

# controller + coordinator + launcher

of the complete Hadoop MapReduce system.
------------------------------------------------------------------
# Complete Deep Explanation of Hadoop Commands

Now you are learning the:

# practical execution commands

These commands:

* compile Java Hadoop code
* create JAR file
* create input data
* upload file to HDFS
* run MapReduce job
* read output from HDFS

This is the complete practical workflow of a Hadoop MapReduce program.

We will understand:

* every word
* every symbol
* every command
* shell behavior
* HDFS interaction
* JVM working
* Hadoop runtime
* classpath
* JAR structure
* distributed execution
* complete internal process

VERY deeply.

---

# COMPLETE FLOW

```text id="ot6tsc"
Java Code
   ↓
Compilation
   ↓
.class Files
   ↓
JAR Packaging
   ↓
Input Creation
   ↓
Upload to HDFS
   ↓
Run Hadoop Job
   ↓
MapReduce Execution
   ↓
Output Stored in HDFS
   ↓
View Output
```

---

# COMMAND 1 — COMPILATION

```bash id="9jlwmn"
javac -classpath `hadoop classpath` -d . WCMapper.java WCReducer.java WCDriver.java
```

VERY IMPORTANT COMMAND.

---

# STEP 1 — What is javac?

```bash id="yjlwmq"
javac
```

means:

# Java Compiler

---

# What Does Compiler Do?

Converts:

```text id="djlwmq"
.java source code
```

into:

```text id="fjlwmv"
.class bytecode
```

---

# Example

Before:

```text id="jlwmau"
WCMapper.java
```

After:

```text id="jlwm5f"
WCMapper.class
```

---

# Why Compilation Needed?

JVM cannot directly understand:

```java id="hjlwm2"
.java
```

It understands:

```text id="2jlwmv"
bytecode
```

inside:

```text id="yjlwm7"
.class
```

files.

---

# STEP 2 — -classpath

```bash id="hjlwmn"
-classpath
```

VERY IMPORTANT.

---

# What is Classpath?

Classpath tells Java:

# where required libraries/classes exist

---

# Why Hadoop Needs Classpath?

Your code uses Hadoop classes:

```java id="mjlwm9"
Mapper
Reducer
Text
IntWritable
JobConf
```

These are NOT built into Java.

They exist inside:

# Hadoop JAR libraries

---

# Without classpath

Compiler error occurs:

```text id="xjlwm0"
cannot find symbol
```

because Java cannot locate Hadoop libraries.

---

# STEP 3 — `hadoop classpath`

```bash id="4jlwm1"
`hadoop classpath`
```

VERY IMPORTANT.

---

# What Does This Command Do?

Automatically prints:

# all Hadoop library paths

---

# Example Output

May produce:

```text id="zjlwmx"
/usr/local/hadoop/share/hadoop/common/*
/usr/local/hadoop/share/hadoop/mapreduce/*
...
```

---

# Why Backticks Used?

```bash id="cjlwmu"
` `
```

means:

# command substitution

Shell executes inner command first.

---

# Internal Working

---

## Shell First Executes

```bash id="gjlwmh"
hadoop classpath
```

---

## Gets Huge Library Path

Example:

```text id="yjlwmz"
/path1:/path2:/path3
```

---

## Final Expanded Command

Becomes:

```bash id="1jlwm6"
javac -classpath /path1:/path2:/path3 ...
```

---

# STEP 4 — -d .

```bash id="8jlwm9"
-d .
```

---

# What is -d ?

Means:

# destination directory

---

# . (dot)

Represents:

# current directory

---

# Meaning

Store generated:

```text id="3jlwmj"
.class files
```

in current folder.

---

# STEP 5 — Java Files

```bash id="hjlwmt"
WCMapper.java
WCReducer.java
WCDriver.java
```

These are source files.

---

# Compiler Generates

```text id="7jlwmw"
WCMapper.class
WCReducer.class
WCDriver.class
```

---

# COMPLETE INTERNAL COMPILATION FLOW

```text id="zjlwml"
Java Source Code
      ↓
javac Compiler
      ↓
Syntax Checking
      ↓
Bytecode Generation
      ↓
.class Files
```

---

# COMMAND 2 — CREATE JAR FILE

```bash id="0jlwm8"
jar -cvf wordcount.jar *.class
```

VERY IMPORTANT.

---

# What is JAR?

# Java Archive File

A compressed package containing:

* class files
* metadata
* resources

---

# Why Hadoop Uses JAR?

Hadoop distributes:

# JAR file across cluster nodes

---

# STEP 1 — jar

```bash id="zjlwmf"
jar
```

Java archive tool.

---

# STEP 2 — -cvf

VERY IMPORTANT.

---

# c

```bash id="2jlwmk"
c
```

means:

# create

Create new JAR.

---

# v

```bash id="4jlwm3"
v
```

means:

# verbose

Show detailed output.

---

# f

```bash id="zjlwm4"
f
```

means:

# file

Specify filename.

---

# STEP 3 — wordcount.jar

Output JAR filename.

---

# STEP 4 — *.class

VERY IMPORTANT.

---

# What is * ?

Wildcard.

Means:

# all matching files

---

# *.class Means

Include:

```text id="8jlwm2"
all .class files
```

---

# Example

```text id="5jlwmm"
WCMapper.class
WCReducer.class
WCDriver.class
```

---

# INTERNAL JAR CREATION FLOW

```text id="0jlwm5"
.class Files
     ↓
Compression
     ↓
Packaging
     ↓
wordcount.jar
```

---

# Why JAR Important in Hadoop?

Hadoop sends JAR:

* across cluster
* to worker nodes
* for mapper/reducer execution

---

# COMMAND 3 — CREATE INPUT FILE

```bash id="7jlwm4"
nano input.txt
```

---

# What is nano?

# Terminal text editor

Used to:

* create files
* edit files

---

# input.txt

Text file containing data.

---

# After Writing

```text id="6jlwm3"
hadoop is big data
hadoop is useful
big data is powerful
```

This becomes:

# input dataset

---

# Save and Exit

---

## CTRL + O

Save file.

---

## CTRL + X

Exit nano.

---

# COMMAND 4 — CREATE HDFS DIRECTORY

```bash id="9jlwm2"
hdfs dfs -mkdir /input
```

VERY IMPORTANT.

---

# hdfs

Hadoop Distributed File System command.

---

# dfs

Means:

# distributed filesystem operations

---

# -mkdir

Means:

# make directory

---

# /input

Directory name in HDFS.

---

# IMPORTANT

This directory is:

# NOT local filesystem

It exists inside:

# HDFS

---

# INTERNAL WORKING

---

## Command Sent to NameNode

NameNode updates:

```text id="1jlwm0"
filesystem metadata
```

---

## Creates HDFS Directory Entry

```text id="8jlwm7"
/input
```

---

# COMMAND 5 — Upload File to HDFS

```bash id="3jlwm7"
hdfs dfs -put input.txt /input
```

VERY IMPORTANT.

---

# -put

Means:

# upload local file to HDFS

---

# Source

```text id="9jlwm5"
input.txt
```

local file.

---

# Destination

```text id="zjlwm9"
/input
```

HDFS directory.

---

# INTERNAL WORKING OF -put

VERY IMPORTANT.

---

# STEP 1 — Client Contacts NameNode

Requests:

```text id="0jlwm1"
store file
```

---

# STEP 2 — NameNode Decides Block Locations

Example:

```text id="6jlwm9"
Block1 → DataNode1
Block2 → DataNode2
```

---

# STEP 3 — File Split into Blocks

Default:

```text id="5jlwm0"
128 MB
```

---

# STEP 4 — Blocks Sent to DataNodes

Stored distributedly.

---

# STEP 5 — Replication

Copies created.

Example:

```text id="3jlwmz"
3 replicas
```

---

# FINAL RESULT

File now exists in:

# HDFS

---

# COMMAND 6 — List Files in HDFS

```bash id="4jlwmn"
hdfs dfs -ls /input
```

---

# -ls

Means:

# list files

---

# Purpose

Verify upload succeeded.

---

# Example Output

```text id="yjlwm8"
input.txt
```

---

# COMMAND 7 — RUN MAPREDUCE PROGRAM

```bash id="4jlwmt"
hadoop jar wordcount.jar WCDriver /input /output
```

MOST IMPORTANT COMMAND.

---

# COMPLETE STRUCTURE

```bash id="6jlwm7"
hadoop jar <jarfile> <mainclass> <input> <output>
```

---

# STEP 1 — hadoop

Runs Hadoop framework.

---

# STEP 2 — jar

Tells Hadoop:

```text id="4jlwm5"
execute JAR application
```

---

# STEP 3 — wordcount.jar

Contains:

* mapper class
* reducer class
* driver class

---

# STEP 4 — WCDriver

Main driver class.

Hadoop internally calls:

```java id="0jlwm7"
main()
```

inside:

```java id="zjlwm1"
WCDriver
```

---

# STEP 5 — /input

Input HDFS directory.

---

# STEP 6 — /output

Output HDFS directory.

Reducer output stored here.

---

# COMPLETE INTERNAL EXECUTION FLOW

VERY IMPORTANT.

---

# STEP 1 — Hadoop Starts JVM

Loads:

```text id="1jlwm9"
wordcount.jar
```

---

# STEP 2 — WCDriver.main() Executes

---

# STEP 3 — JobConf Created

Stores configuration.

---

# STEP 4 — Input Splits Created

Input file divided into blocks.

---

# STEP 5 — Mapper Tasks Assigned

Usually:

```text id="4jlwm0"
1 split = 1 mapper
```

---

# STEP 6 — Mapper Execution

Generates:

```text id="3jlwm4"
(word,1)
```

pairs.

---

# Example Mapper Output

```text id="9jlwm1"
(hadoop,1)
(is,1)
(big,1)
(data,1)
```

---

# STEP 7 — Shuffle Phase

Groups identical keys.

---

# Example

```text id="8jlwm5"
(hadoop,[1,1])
(is,[1,1,1])
```

---

# STEP 8 — Sort Phase

Alphabetical sorting.

---

# STEP 9 — Reducer Execution

Calculates totals.

---

# Example

```text id="7jlwm1"
(hadoop,2)
```

---

# STEP 10 — Output Stored in HDFS

Inside:

```text id="2jlwm9"
/output
```

---

# COMMAND 8 — VIEW OUTPUT

```bash id="9jlwm7"
hdfs dfs -cat /output/part-00000
```

VERY IMPORTANT.

---

# -cat

Means:

# display file contents

---

# What is part-00000?

Reducer output file.

---

# Why Named part-00000?

Hadoop reducers generate:

```text id="6jlwm8"
part files
```

---

# Example

| Reducers   | Output Files           |
| ---------- | ---------------------- |
| 1 reducer  | part-00000             |
| 2 reducers | part-00000, part-00001 |

---

# FINAL OUTPUT

```text id="5jlwm6"
big 2
data 2
hadoop 2
is 3
powerful 1
useful 1
```

---

# COMPLETE END-TO-END FLOW

```text id="4jlwm8"
Java Files
   ↓
javac
   ↓
.class Files
   ↓
jar
   ↓
wordcount.jar
   ↓
Upload Input to HDFS
   ↓
Run Hadoop Job
   ↓
Mapper Execution
   ↓
Shuffle & Sort
   ↓
Reducer Execution
   ↓
Output in HDFS
   ↓
View Output
```

---

# IMPORTANT HADOOP CONCEPTS USED

| Concept               | Used In          |
| --------------------- | ---------------- |
| Compilation           | javac            |
| Classpath             | Hadoop libraries |
| Packaging             | JAR              |
| HDFS Upload           | put              |
| Distributed Storage   | HDFS             |
| Job Submission        | hadoop jar       |
| Distributed Execution | Mapper/Reducer   |
| Aggregation           | Reducer          |
| Output Retrieval      | cat              |

---

# VERY IMPORTANT INTERVIEW/EXAM CONCEPTS

---

# Why JAR Needed?

Because Hadoop distributes application across cluster nodes.

---

# Why Classpath Needed?

Compiler must locate Hadoop libraries.

---

# Why HDFS Used?

MapReduce operates on distributed filesystem.

---

# Why Output Directory Must Not Exist?

Hadoop prevents accidental overwrite.

---

# Why part-00000?

Reducer output stored as partitioned files.

---

# FINAL UNDERSTANDING

These commands together demonstrate:

* Java compilation
* Hadoop library linking
* distributed storage
* HDFS interaction
* job packaging
* MapReduce execution
* cluster computing
* distributed processing
* output retrieval

This is the complete practical lifecycle of a Hadoop MapReduce application.
